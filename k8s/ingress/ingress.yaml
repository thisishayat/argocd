apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ostad-capstone-ingress
  namespace: prod
  labels:
    app: ostad-capstone
    environment: prod
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
spec:
  tls:
  - hosts:
    - ostad-capstone-prod.com
    - www.ostad-capstone-prod.com
    secretName: ostad-capstone-tls
  rules:
  - host: ostad-capstone-prod.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ostad-capstone-prod-service
            port:
              number: 80
  - host: www.ostad-capstone-prod.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ostad-capstone-prod-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ostad-capstone-stage-ingress
  namespace: stage
  labels:
    app: ostad-capstone
    environment: stage
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/rate-limit: "50"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  rules:
  - host: ostad-capstone-stage.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ostad-capstone-stage-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ostad-capstone-dev-ingress
  namespace: dev
  labels:
    app: ostad-capstone
    environment: dev
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: ostad-capstone-dev.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ostad-capstone-dev-service
            port:
              number: 80
